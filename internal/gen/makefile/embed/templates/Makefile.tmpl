{{- define "output" -}}bin/{{ . }}{{- end -}}
{{- define "reg_inputs" -}}src/{{ . }}/main.cpp src/rtcg/rtcg.cpp{{- end -}}
{{- define "convert" -}}{{- if .HasConversion }} src/convert.cpp{{- end -}}{{- end -}}

.POSIX:

CXXFLAGS = {{ block "cxxflags" . }}-std=c++11 -Wall -Wextra{{ end }}

.PHONY: all mkdir

all: mkdir {{- range $k, $v := .Tests }} {{ template "output" $k }}{{- end }}

mkdir:
	mkdir -p bin
{{ range $k, $v := .Tests }}
{{ template "output" $k }}: {{ template "reg_inputs" $k }} {{- template "convert" $ }}
	$(CXX) $(CXXFLAGS) -o $@ {{ template "reg_inputs" $k }} {{- template "convert" $ -}}
{{- end }}
